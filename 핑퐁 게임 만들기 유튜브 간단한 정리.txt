핑퐁:하얀색 공이 날아다닌다
키보드의 방향키로 아랫쪽 막대바로 조종을 한다
처음에 3개의 공이 주어진다.
떨어질시 life가 1씩 감소함
lifef3개가 감소할시 게임이 끝나는걸로 된다

import turtle로 터틀 모듈을 불러온다
랜덤모듈과
타임모듈 불러옴
비지컬러로 백그라운드 색깔을 만든다
setup으로 가로 세로 길이를 정해준다
플레이어 객체를 만든다 
코드:t.Turtle()
플레이어의 객체의 shape을 아래 막대바로 만든다

그 다음 플레이어(막대바)가 키보드로 움직일 수 있도록
그래픽 창에
t.listen()
t.onkeypress
으로 오른쪽 왼쪽 키를 조정할 수 있도록 한다

볼

ball이라는 객체 t.Turtle()
ball.shape("circle")

ball_yspeed로 속도 조정

이제는 상단의 LIFE라는 변수 3개의 생명 
점수 표시 
t.up()
t.ht()

